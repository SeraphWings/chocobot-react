import React from 'react';
import ReactDOM from 'react-dom';

class Textlog extends React.Component {
    render(){
        return(
            const logs = this.props.dialog.map( (element) => {
                <li>{element}</li>
            }

        )
    }
}

export default Textlog;
